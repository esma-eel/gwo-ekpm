import math

def move(wolf, prev_t, current_t):
    """
    position in prev_t --> t
    position in current_t --> t+1
    for j in positions:
        positions[current_t][j] - positions[prev_t][j]
    """